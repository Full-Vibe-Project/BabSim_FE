// src/features/sync/ui/NotionSync.tsx
type NotionSyncProps = {
  isConnected: boolean;
  databaseName?: string;
};

export default function NotionSync({ isConnected, databaseName }: NotionSyncProps) {
  return null;
}
